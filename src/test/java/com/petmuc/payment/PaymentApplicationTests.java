package com.petmuc.payment;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.ActiveProfiles;

@ActiveProfiles("test")
@SpringBootTest
class PaymentApplicationTests {

    @Test
     void contextLoads() {
        // This method is intentionally left empty. It serves as a placeholder
        // for verifying that the Spring application context loads successfully.
        // If the application context fails to load, this test will fail, indicating
        // an issue with the application's configuration or dependencies.

        // Uncomment the line below if this test should indicate that the operation is not supported.
    }
}
